# audio/ (voice_recorder, transcribe_audio)
numpy>=1.24.0
sounddevice>=0.4.6
soundfile>=0.12.0
scipy>=1.10.0

# audio/transcribe_audio (whisper + VAD)
# GPU: install torch+torchaudio from PyTorch CUDA index first
torch>=2.4.0
torchaudio>=2.4.0
openai-whisper>=20231117
silero-vad

# config (env loading)
python-dotenv>=1.0.0

# llm (generateresponse.py)
openai>=1.0.0

# vision (vision.py)
opencv-python>=4.8.0

# Optional (tts, etc.) — see docs/AI_Custom_Voice_Personality_Guide.md
# pydub
# mediapipe>=0.10.0
# elevenlabs
